# Cat vs Dog Classifier (FastAPI + PyTorch)

This project implements a binary image classification system (Cat vs Dog)
using PyTorch and serves predictions through a FastAPI inference API.
The application is containerized using Docker.

---

## 🐳 Docker Setup

### 1️⃣ Build Docker Image

Run the following command in the project root directory:

```bash
docker build -t cat-dog-classifier .
# Cat_Dog_Classifier
